import { labeledLog } from './console-logger';
import { waitElementRender } from './wait-element-render';
import { isAlreadyRunningExtension } from './extension-running-checker';

export const webExtensionUtils = {
  labeledLog,
  waitElementRender,
  isAlreadyRunningExtension,
}
